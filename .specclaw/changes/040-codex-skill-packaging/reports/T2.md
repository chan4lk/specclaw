# T2 — Create the repository-local Codex SpecClaw adapter

Added the single `.agents/skills/specclaw/SKILL.md` adapter. It resolves the
checkout's canonical plugin root, routes a request to the existing verb skill,
uses the canonical `bin/` helpers, and maps `CLAUDE_PLUGIN_ROOT` references to
that canonical root. No lifecycle files were copied.

## Verification

Command: `bash plugins/specclaw/tests/run-codex-skill-tests.sh`
Exit: 0
