# T1 — Add failing validation for the Codex skill adapter

Created `run-codex-skill-tests.sh` to require a single valid Codex adapter,
canonical skill and binary references, Claude-root compatibility, canonical
verb availability, and no duplicated asset trees. The test first failed because
the adapter did not exist, then passed once the adapter supplied the expected
canonical references.

## Verification

Command: `bash plugins/specclaw/tests/run-codex-skill-tests.sh`
Exit: 0
