# Prototype Brief: {{title}}

**Date generated:** {{date}}
**Plugin version:** {{plugin_version}}
**Legacy commit:** {{legacy_commit}}

{{stack_header}}

<!--
  THE HAND-OFF DOCUMENT. specclaw does not contain a UI generator and never
  will: this file is what it produces, and a separate prototype/UI skill
  (config.yaml's `prototype.skill`) is what builds from it.

  Fully regenerated by /specclaw:bf-prototype on every run. NEVER hand-edited —
  an edit here is lost on the next run, and worse, it would be an unrecorded
  change to what the client is being asked to approve. Approvals live in
  prototype-approvals.md, which no agent or script ever writes.

  NOT copied to the new repo. The new repo reads prototype-manifest.json,
  which carries everything it needs — including the review points — precisely
  so that nothing downstream depends on this file surviving.

  ── THE STACK IS NOT A CHOICE MADE HERE ────────────────────────────────────
  The framework and the language are both resolved from the decision record,
  each with the id, decider and date that sanction it. The brief states them;
  it does not pick them, and neither does the skill it hands off to. If either
  is missing, /specclaw:bf-prototype stops before this file is written — it
  never defaults the language, not from the framework's convention and not
  from anything else. See SQ-006 and SQ-015.

  ── WHAT EVERY SCREEN SECTION MUST CITE ────────────────────────────────────
  Every field, rule and workflow step carries evidence: an SCR-### from
  ui-inventory.md, a DR-### from domain-model.md, a BL-### from
  rebuild-backlog.md, each with a file:line or a named document section. A
  claim the agent cannot cite is NOT written as prose and quietly built — it
  raises a PQ-### in .specclaw/analysis/pending-questions.md and the screen
  renders PROVISIONAL(PQ-###) until a human answers it.

  ── BEHAVIOUR CHANGES ARE QUESTIONS, NOT DESIGN CHOICES ────────────────────
  Anything the redesign would change about what the system DOES — a required
  field removed or added, a validation altered, a workflow merged or split, a
  status transition changed, a screen retired — is emitted as a PQ-###, never
  written inline as a decided change. This command never allocates a CQ
  number; /specclaw:bf-clarify promotes the PQ and a human decides it. Until
  then bash refuses to compute the affected screen APPROVED, which is what
  stops a redesign quietly changing the system on its way to sign-off.

  ── THE MACHINE DIRECTIVES BLOCK IS A CONTRACT ─────────────────────────────
  The final section is read by bash, line by line, and nothing else in this
  document is. Prose is for the humans and the prototype skill; the directives
  are for specclaw-bf-prototype record. A screen with no PS: line does not
  exist as far as the manifest, the approvals join and every downstream gate
  are concerned. The exact grammar is documented in that section.
-->

## What this prototype is for

{{purpose}}

<!--
  Two or three sentences, in the client's language. What the redesign is
  trying to achieve, and the fact that this prototype is a throwaway built so
  the client can approve real screen behaviour before anybody builds the
  application. It is design-acceptance code: read as a reference during the
  build, never copied into the new repo, and never the production foundation.
-->

## Inputs this brief was built from

{{inputs_table}}

<!--
  Bash-supplied: every input document with its sha256 at the time of this run,
  so a reader can tell whether the brief still describes the analysis it was
  derived from. Never hand-written.
-->

## Out of scope for the prototype

The prototype must not fake authentication, must not persist anything, and must
not stand in for real data. Static in-memory sample data is expected and must be
visibly labelled as sample data. No backend, no database, no authentication, no
network calls.

{{out_of_scope_notes}}

## Screens

{{screen_sections}}

<!--
  One section per prototype screen, in PS-### order:

  ### PS-00N — <screen name>

  - **Replaces:** SCR-### — <legacy screen title> (or: NEW — sanctioned by
    CQ-###, or: NEW — PROVISIONAL(PQ-###) while the question is open)
  - **Module:** MOD-###
  - **Serves:** BL-### (<in scope | out of scope: reason>)
  - **Fields:** a table — name, type, required, validation — every row citing
    the DR-### or file:line that evidences it.
  - **Workflow steps:** the ordered steps a user takes, each cited.
  - **Rules visible on this screen:** DR-### list, each with what the user
    actually sees of it.
  - **Tokens:** global TK- groups only. A REINTERPRET project has no
    per-screen token obligation — the legacy look is reference material.
  - **Review points:** short, human-checkable statements about layout or flow,
    each naming the DR-### or workflow step it protects. These are what the
    new repo's ui-review.md turns into sign-off rows, so write them as things
    a person can look at a screen and confirm — not as implementation notes.
  - **Proposed behaviour changes:** PQ-###/CQ-### references ONLY, with one
    line each on what the question is. Never the change itself as prose.
-->

## Screens not carried forward

{{screens_retired}}

<!--
  One line per legacy screen the redesign drops, each citing the DECIDED CQ-###
  that retired it. A screen dropped on an undecided question does not belong
  here — it belongs in pending-questions.md, and bash will not count it
  retired until the CQ is decided.
-->

## Screens out of rebuild scope

{{screens_out_of_scope}}

<!--
  One line per legacy screen no active backlog item renders, citing the
  backlog's own scope statement. These never block readiness — a client who
  scoped a screen out has already decided about it.
-->

## Hand-off to the prototype skill

{{handoff_block}}

<!--
  Bash-supplied stack values, then these instructions verbatim:

  - Build in the stated framework and the stated language ONLY. Do not choose
    either, do not substitute either, and do not add a second language. If you
    cannot build in both as stated, stop and say so — a prototype in a
    different stack is not the design anybody is approving.
  - Build only from the screen sections above. No invented screens, no
    invented fields, no invented rules.
  - One route/screen per PS-###, with `data-testid="PS-###"` on each screen's
    root element.
  - No backend, no database, no authentication, no network calls. Static
    in-memory sample data only, visibly labelled.
  - This code is discarded after approval. Do not structure it as a production
    foundation, do not add build/deploy tooling beyond what is needed to view
    it, and do not optimise it for reuse.
-->

## Machine Directives

<!--
  READ BY BASH. One directive per line, pipe-delimited, no blank lines inside
  a directive. Everything above this heading is for humans and the prototype
  skill; everything here is the contract with specclaw-bf-prototype record.

    PS: <PS-###> | <SCR-### it replaces, or NEW> | <MOD-###> | <BL-###,...> | <DR-###,...>
    REVIEW-POINT: <PS-###> | <DR-### or workflow:<step> it protects> | <the statement>
    PROVISIONAL: <PS-###> | <PQ-### or CQ-###>
    RETIRED-SCR: <SCR-###> | <the DECIDED CQ-### that retired it>
    LANG-FILE-CONVENTIONS: allow=<ext>,<ext> forbid=<ext>,<ext>

  Notes that matter:

  - PS ids come from `specclaw-bf-prototype allocate` and nowhere else. Never
    invent one, never renumber one, never reuse one.
  - A PS: line is what makes a screen exist to the manifest. Omit it and the
    screen is invisible to every gate downstream, however much prose it has.
  - PROVISIONAL takes a PQ or a CQ. Bash checks the PQ's Status line and the
    CQ's decided state itself — asserting "decided" here would achieve
    nothing, because bash does not read this field for that.
  - RETIRED-SCR is only honoured when the cited CQ is actually decided.
  - LANG-FILE-CONVENTIONS is how the resolved language is enforced against the
    built tree. The extensions come from the DECIDED language, stated here by
    the agent; bash holds no table of languages or extensions and enforces
    nothing when this line is absent. `forbid` is the operative half: it names
    the extensions that would mean the prototype was built in some other
    language than the one decided.
-->

{{machine_directives}}

---

_Generated by `/specclaw:bf-prototype`. Regenerated in full on every run — never hand-edit. Approvals are recorded by a named human in `prototype-approvals.md`, which no agent or script writes._
