# Redesigned screens — approval record

**{{ready_line}}**

**Built in:** {{stack}}

**Screens:** {{counts}}

{{narration}}

## Screens

| Screen | Replaces | Module | Backlog items | Rules shown | Proposed changes | Status | Approved by | Date |
|---|---|---|---|---|---|---|---|---|
{{rows}}

{{retired_section}}
## What the statuses mean

| Status | Meaning |
|---|---|
| Approved | A named client stakeholder signed this screen off, against exactly the prototype recorded here. |
| Awaiting a decision | The redesign proposes a change to what the system does on this screen, and that change is still an open question. The screen cannot be approved until it is answered. |
| Changed since approval | The prototype was edited after it was signed off, so the approval no longer describes what exists. It needs re-approving. |
| In review | Captured and ready to be looked at; nobody has signed it yet. |
| Changes requested | Reviewed and sent back with changes. |
| Not approved | Reviewed and rejected. |
| Draft | Not yet ready to show. |

---

_Every screen marked approved was approved by the named person, on the date shown, before any of the application was built. Screens the rebuild deliberately leaves out are listed above rather than dropped silently, each with the reason it is out of scope or the decision that retired it._

_Generated {{date}} · plugin {{plugin_version}}_

<!-- Provenance footer: rendered by specclaw-bf-prototype map-render from .specclaw/prototype/prototype-manifest.json, whose values are bash-computed. Fully regenerated on every run — never hand-edit. Approvals are read from prototype-approvals.md, which no agent or script ever writes. -->
